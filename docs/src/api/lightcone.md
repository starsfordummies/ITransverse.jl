# Light Cone

Iterative extension of a temporal MPS along the time direction using the light-cone tMPO.

## Parameter type

```@docs
ConeParams
```

## Initialisation

`init_cone(tp::tMPOParams, N)` – initialise the light-cone temporal MPS given `tMPOParams` and initial length `N`.

## Running the cone

```@docs
run_cone
resume_cone
```
